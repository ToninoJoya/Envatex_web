# .env — variables de entorno para el backend
# Cambia los valores según tu entorno (no subir a git)

# Ruta a la base de datos (por defecto sqlite local)
DATABASE_URL=sqlite:///app.db

# Entorno de Flask: development / production
FLASK_ENV=development

# Puerto en el que correrá la app
PORT="https://didactic-space-fiesta-g4r6x4549q9xfpvq5-5000.app.github.dev"

# Clave secreta de ejemplo (cámbiala en producción)
SECRET_KEY=dev-secret
# JWT secret (opcional). Si no se proporciona, se usará SECRET_KEY.
JWT_SECRET_KEY=dev-jwt-secret

# Credenciales de administrador para login (cambia en producción)
ADMIN_USER=admin
ADMIN_PASSWORD=secreto
